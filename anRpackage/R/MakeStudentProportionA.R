MakeStudentProportionA <-
function(x) {
  # Divides the given data frame variable by the total enrollment at each school
  #   and returns the proportion.
  #
  # Args:
  #   x: Data frame variable
  x1 <- x / a$nces_member
  return(x1)
}

